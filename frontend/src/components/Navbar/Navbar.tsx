import Button from "../Button";
import "./style.css";

const Navbar: React.FC = () => {
    return (
        <nav>
            <Button>Log Off</Button>
        </nav>
    )
};

export default Navbar;
